## ARM, FOOT 모터 조립, 초기 상태
- 본체 - ARM 모터 - ARM Connector - Foot Connector - FOOT 모터 - FOOT 연결
- 본체와 4개 ARM Connector를 연결하는 모터는 혼의 방향이 윗쪽이다.
- 모터의 초기 각도는 90도로 설정하고 ARM는 본체의 대각선상에 위치하도록 조립했다.
- 각 모터의 이름은 A(front-left), B(front-right), C(back-left), D(back-right) 이다.
- ARM 모터는 ARM Connector와 연결되어 있다.
- ARM 모터축이 반시계 방향 회전하므로 ARM Connector도 반시계 방향으로 회전한다.
- 4개의 ARM 모터축이 모두 동일 방향으로 회전하므로 이것을 고려해서 coding해야한다.
- 각 ARM Connector에 연결된 FOOT Connector는 모터의 초기 각도를 120도로 설정하고 FOOT 본체,ARM과 평행하도록 조립했다.(수평)
- Foot Connector에 Foot 모터가 고정되어 있고, Foot은 모터 본체에 고정되어 있다.
  따라서 Foot은 시계 방향 회전한다.

## 관절 동작 범위(모터 입력 각도 기준)
### Arm 
- 개벌 각도는 0도~180도이다. 설계/조립 공차 고러하면 5도~175도이다.
- 인접한 팔이 서로 가까워지는 경우 고려하면 25도 ~ 155도이다.
- 방향 : 방향(위에서 내려볼때 팔 각도-전방 0도, 제어각도)
  - A: 전방(0,45), 대기(45,90), 좌측(90,135)
  - B: 전방(0,135), 대기(-45,90), 우측(-90,45)
  - C: 후방(180,135), 대기(135,90), 좌측(90,45)
  - D: 후방(180,45), 대기(45,90), 우측(-90,135)
  
### Foot
- 개별 각도는 0도~180도이다.
- 개벌 각도는 0도~180도이다. 설계/조립 공차 고러하면 5도~175도이다.
- 인접한 구조물과 간섭은 없다. 
- 방향 : A~D 동일, 방향(수직면 기준 정면관측각도,제어각도)
  - 위-대각(30,180), 수평(90,120), 대기(150,60)
    아래-수직(180,30), 아래-몸쪽(210,0도)

## Stall 동작:제어 각도 기준
- 초기(대기) 자세로 Power On, stall(serial), Init(web) 실행
- FOOT: AF-DF 동시에 현재 위치에서 대기 위치로 회전 
- ARM: AA~DA는 동시에 현재 위치에서 대기 위치로 이동

## WALK 동작 — Crawl V1.0

### 정의
- 보행 방식 이름: **Crawl V1.0** (정적 크리프 보행)
- 한 번에 한 다리만 이동, 나머지 3개는 지면 고정 → 항상 3점 지지 유지
- 가장 느리고 정적으로 안정적인 4족 보행 방식

### 보행 순서
- **A → D → B → C** (front-left → rear-right → front-right → rear-left)
- 대각선 쌍 순차 이동: (A,D) 쌍 → (B,C) 쌍

### 한 다리의 동작 사이클
1. **Swing up** : 발을 FDW(30°) → FUP(60°)로 들어올림
2. **Swing forward** : 팔을 contact 각도까지 전방 이동
3. **Place down** : 발을 FUP(60°) → FDW(30°)로 내려 지면 접촉
4. **Stance push** : 팔을 toeoff 각도까지 후방 이동 (추진력 발생)

### 보행 파라미터 (물리각 기준)
| 다리 | contact | toeoff | arm 방향 |
|------|---------|--------|----------|
| A (앞왼) | 100° | 150° | CW (+1) |
| B (앞오) |  80° |  30° | CCW (-1) |
| C (뒤왼) |  30° |  80° | CW (+1) |
| D (뒤오) | 150° | 100° | CCW (-1) |

- stride: 50° (contact → toeoff 이동 구간)
- FDW: 30° (발 아래, 서있는 자세)
- FUP: 60° (발 든 자세)
- Foot 수평: 120°

### 무게 중심 및 제약
- 제어보드·배터리가 뒷쪽에 위치 → 앞:뒤 무게 비율 = 45:55
- 보행 시 앞뒤로 넘어지지 않도록 무게 중심 확보 필요
- 발을 들기 전 사전 자세 처리(pre-positioning) 필요
- 노젓기(rowing) 방식은 사용하지 않음

### 시뮬레이션
- `simulation/walk_sim.py` — PyBullet 3D 시각화
- 실행: `C:\Python313\python.exe simulation\walk_sim.py`

## Trot 보행 방식
### Refererence 
https://www.youtube.com/watch?v=tLrRlXxM5Yw&t=11s 3:14~3:34 사이 개의 걸음을 프레임 단위로
분석해서 ./docs/trot.csv에 정리했다. 
681~704 프레임이 한 주기이다. 앞 두다리와 뒷 두다리는 3/4 지연일때 지면에 닿는 세 다리가 삼각형을
형성해서 중심을 잡는 구조라 설명되어 있다.
